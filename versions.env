# ==============================================================================
# Backend Version Configuration for neuriplo
# ==============================================================================
#
# This file works in conjunction with scripts/backends.conf to maintain
# consistent backend configuration across the project.
#
# RELATIONSHIP WITH scripts/backends.conf:
# ----------------------------------------
# Each backend defined in scripts/backends.conf must have a corresponding
# version variable defined here. The pairing is validated automatically.
#
# Example pairing:
#   scripts/backends.conf:  "TVM|tvm|TVMInferTest|TVM_VERSION"
#   versions.env (here):    TVM_VERSION=0.18.0
#                                         ^^^^^^^^^^^
#                                         Variable name must match
#
# HOW TO ADD A NEW BACKEND VERSION:
# ----------------------------------
# 1. Add the backend definition to scripts/backends.conf first
# 2. Add the version variable here (must match the VERSION_VAR_NAME from backends.conf)
# 3. The validation system will verify the pairing is correct
#
# Example:
#   If backends.conf has: "NCNN|ncnn|NCNNInferTest|NCNN_VERSION"
#   Add here:             NCNN_VERSION=1.0.34
#
# VALIDATION:
# -----------
# Run validation to check consistency:
#   source scripts/backends.conf && validate_backend_versions
#
# Or run cmake (validation happens automatically):
#   cmake ..
#
# ==============================================================================

# Backend versions
# Note: Variable names must match those defined in scripts/backends.conf
OPENVINO_VERSION=2025.2.0      # Used by: OPENVINO backend
TENSORFLOW_VERSION=2.19.0      # Used by: LIBTENSORFLOW backend
PYTORCH_VERSION=2.3.0          # Used by: LIBTORCH backend
TENSORRT_VERSION=10.7.0.23     # Used by: TENSORRT backend
ONNX_RUNTIME_VERSION=1.19.2    # Used by: ONNX_RUNTIME backend
OPENCV_VERSION=4.6.0           # Used by: OPENCV_DNN backend
GGML_VERSION=master            # Used by: GGML backend
TVM_VERSION=0.18.0             # Used by: TVM backend

# CUDA version for GPU backends
CUDA_VERSION=12.6


# Docker configuration
DOCKER_BASE_IMAGE=ubuntu:24.04
DOCKER_USER=testuser
DOCKER_WORKDIR=/app
